import './button.css';

import { Link } from 'react-router-dom';

const Button = ({text, location, path}) => {
    return (
        <Link onClick={() => {window.scrollTo(0,0)}} to={path} className='button'>
            {text}
            <img src={location} alt="Изображение" className='button__icon'></img>
        </Link>
    );
}

export default Button;